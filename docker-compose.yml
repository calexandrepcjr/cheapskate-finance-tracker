services:
  cheapskate:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - cheapskate-data:/app/data
    restart: unless-stopped
    environment:
      - TZ=UTC

volumes:
  cheapskate-data:
    driver: local
