<?xml version="1.0" encoding="utf-8"?>
<!--
    network_security_config.xml

    Android 9 (Pie, API 28) and above block cleartext (http://) traffic by default.
    The local Go server runs on http://localhost:8080, so we must explicitly permit
    cleartext traffic to localhost / 127.0.0.1.

    All other domains use the system defaults (HTTPS required).

    Reference:
      https://developer.android.com/training/articles/security-config
-->
<network-security-config>

    <!-- Allow cleartext HTTP only to the local loopback address. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!--
        Base config: all other connections must use HTTPS.
        This is Android's default behaviour; it is written out explicitly for clarity.
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

</network-security-config>
